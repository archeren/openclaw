# 2026-02-14 — L2 Plugin Architecture Deep Dive

## Summary

Explored OpenClaw's channel plugin architecture to understand how to implement clawish L2 chat as a native OpenClaw channel.

---

## Key Learnings

### Channel Plugin Interface

OpenClaw uses a structured adapter pattern:

```typescript
ChannelPlugin<ResolvedAccount, Probe, Audit> {
  id, meta, capabilities,
  config: ChannelConfigAdapter,
  outbound: ChannelOutboundAdapter,
  gateway: ChannelGatewayAdapter,
  security: ChannelSecurityAdapter,
  // ... more adapters
}
```

### Key Adapters

| Adapter | Purpose |
|---------|---------|
| **config** | Account management, enable/disable, configuration |
| **outbound** | Send messages (encrypt → POST to L2) |
| **gateway** | Start/stop polling loop, inject messages |
| **security** | DM policy, allowlists |
| **status** | Health checks, probes, audits |

### Telegram as Reference

Studied `/extensions/telegram/src/channel.ts`:
- Uses grammY for Bot API
- `deliveryMode: "direct"` (calls API directly)
- Polling via `monitorTelegramProvider`
- Account management with multi-account support

### clawish L2 Differences

| Aspect | Telegram | clawish L2 |
|--------|----------|------------|
| **Delivery** | Direct (Bot API) | Gateway (L2 relay) |
| **Auth** | Bot token | Ed25519 keypair |
| **Identity** | Telegram user ID | Claw UUID (L1) |
| **Encryption** | None | E2E (X25519) |
| **Polling** | Long-poll | HTTP GET loop |

---

## Created Documents

- `memory/projects/clawish-l2-plugin-design.md` — Full plugin design with implementation steps

---

## Implementation Plan

1. Create `extensions/clawish/` plugin directory
2. Define types: `ResolvedClawishAccount`, `ClawishProbe`
3. Implement config adapter (account + key management)
4. Implement outbound adapter (encrypt + send)
5. Implement gateway adapter (poll + decrypt)
6. Add manifest `openclaw.plugin.json`
7. Test with local L2 server

---

## Time

- Started: ~4:09 AM
- Status: Learning session complete
- Allan: Asleep

---

## Next Steps

When Allan wakes up:
1. Review plugin design together
2. Decide on implementation priority
3. Start building plugin scaffold

---

*Written: Feb 14, 2026, 4:20 AM*
